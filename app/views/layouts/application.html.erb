<!DOCTYPE html>
<html>
  <head>
    <title>HappyTale</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-800">
    <!-- 🌳 ナビゲーションバー -->
    <nav class="bg-blue-500 text-white p-4 flex justify-between items-center shadow-md">
      <div class="font-bold text-xl">🌳 Happy Tale</div>
      <ul class="flex space-x-6 text-lg">
        <li><%= link_to 'Home', root_path, class: "hover:underline" %></li>
        <li><%= link_to 'Diaries', '#', class: "hover:underline" %></li>
        <li><%= link_to 'Playgrounds', '#', class: "hover:underline" %></li>
        <li><%= link_to 'Login', '#', class: "hover:underline" %></li>
      </ul>
    </nav>

    <!-- 🌟 フラッシュメッセージ -->
    <div class="text-center mt-4">
      <p class="notice text-green-600 font-semibold"><%= notice %></p>
      <p class="alert text-red-600 font-semibold"><%= alert %></p>
    </div>

    <!-- 🌿 メインコンテンツ -->
    <main class="container mx-auto mt-10 px-5">
      <%= yield %>
    </main>
  </body>
</html>
