<!DOCTYPE html>
<html>
  <head>
    <title>HappyTale</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-800">
    <!-- 🌳 ナビゲーションバー（TOPページと共通） -->
    <nav class="bg-amber-400 text-white shadow-md sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- ロゴ -->
          <%= link_to "🌳 Happy Tale", root_path, class: "font-bold text-2xl hover:text-amber-100" %>

          <!-- デスクトップメニュー -->
          <ul class="hidden md:flex space-x-6 text-lg font-medium">
            <li><%= link_to 'Home', root_path, class: "hover:text-amber-100 transition-colors" %></li>
            <% if user_signed_in? %>
              <li>
                <%= link_to 'Logout', destroy_user_session_path,
                    method: :get,
                    data: { turbo: false, confirm: "ログアウトしますか？" },
                    class: "hover:text-amber-100 transition-colors" %>
              </li>
            <% else %>
              <li><%= link_to 'Login', new_user_session_path, data: { turbo: false }, class: "hover:text-amber-100 transition-colors" %></li>
              <li><%= link_to 'Sign Up', new_user_registration_path, data: { turbo: false }, class: "hover:text-amber-100 transition-colors" %></li>
            <% end %>
          </ul>

          <!-- モバイルハンバーガーアイコン -->
          <button id="menu-btn" class="md:hidden focus:outline-none">
            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </div>

      <!-- モバイルメニュー -->
      <div id="mobile-menu" class="hidden md:hidden bg-amber-300 text-gray-800 px-6 pb-4 space-y-3">
        <%= link_to 'Home', root_path, class: "block hover:text-amber-600" %>
        <% if user_signed_in? %>
          <%= link_to 'Logout', destroy_user_session_path,
              method: :get,
              data: { turbo: false, confirm: "ログアウトしますか？" },
              class: "block hover:text-amber-600" %>
        <% else %>
          <%= link_to 'Login', new_user_session_path, data: { turbo: false }, class: "block hover:text-amber-600" %>
          <%= link_to 'Sign Up', new_user_registration_path, data: { turbo: false }, class: "block hover:text-amber-600" %>
        <% end %>
      </div>
    </nav>

    <!-- 🌟 フラッシュメッセージ -->
    <div class="text-center mt-4">
      <p class="notice text-green-600 font-semibold"><%= notice %></p>
      <p class="alert text-red-600 font-semibold"><%= alert %></p>
    </div>

    <!-- 💫 Devise画面の中身 -->
    <main class="container mx-auto mt-8 px-4">
      <%= yield %>
    </main>

    <!-- 📱 ハンバーガーメニュー開閉スクリプト -->
    <script>
      const menuBtn = document.getElementById('menu-btn');
      const mobileMenu = document.getElementById('mobile-menu');
      menuBtn.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
      });
    </script>
  </body>
</html>
