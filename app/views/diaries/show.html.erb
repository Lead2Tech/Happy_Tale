<div class="max-w-3xl mx-auto mt-16 bg-white p-8 rounded-2xl shadow-md">
  <h1 class="text-3xl font-bold text-center text-gray-800 mb-8">
    🌱 <%= @diary.title %>
  </h1>

  <div class="text-gray-700 space-y-6">
    <p class="text-lg whitespace-pre-line"><%= @diary.content %></p>

    <% if @diary.visited_at.present? %>
      <p class="text-sm text-gray-500">🗓 訪問日: <%= @diary.visited_at.strftime("%Y年%m月%d日") %></p>
    <% end %>

    <% if @diary.playground_id.present? %>
      <p class="text-sm text-gray-500">🏞 遊び場ID: <%= @diary.playground_id %></p>
    <% end %>
  </div>

  <div class="flex justify-between mt-10">
    <%= link_to "✏️ 編集する", edit_diary_path(@diary),
        class: "bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-6 rounded-lg shadow-md transition transform hover:-translate-y-0.5" %>

    <%= link_to "🗑 削除する", diary_path(@diary),
        data: { turbo_method: :delete, confirm: "本当に削除しますか？" },
        class: "bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-6 rounded-lg shadow-md transition transform hover:-translate-y-0.5" %>
  </div>

  <div class="text-center mt-6">
    <%= link_to "← 日記一覧へ戻る", diaries_path,
        class: "text-blue-600 hover:underline text-sm" %>
  </div>
</div>
